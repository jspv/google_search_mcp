# Project defaults for Lambda/Gateway deployment 
# No secrets here. Local overrides and discovered values live in .deploy.env

# CloudFormation template and artifact
# Paths may be relative to this file's directory (deploy_aws_agentcore_auth0/)
TEMPLATE_FILE="cloudformation-lambda.yaml"
ARTIFACT_PATH="../dist/lambda.zip"
BUILD_CMD="./build_zip.sh"
ARTIFACT_NAME_BASIS="lambda_artifact"   # used to form S3 key name

# Lambda defaults
LAMBDA_FUNCTION_NAME=""
LAMBDA_ARCHITECTURE="arm64"   # arm64 | x86_64
LAMBDA_TIMEOUT="60"
LAMBDA_MEMORY="512"
LAMBDA_LOG_RETENTION="14"
LAMBDA_CAPABILITIES="CAPABILITY_IAM"

# Map to CFN parameters (auto-collected by deploy script)
CFN_PARAM_FunctionName="${LAMBDA_FUNCTION_NAME}"
CFN_PARAM_Architecture="${LAMBDA_ARCHITECTURE}"

# Provider-specific params (resolved from .env at runtime)
# Example: CFN_PARAM_SomeConfig="${SOME_CONFIG:-}"

# Outputs mapping
CFN_OUTPUT_FUNCTION_ARN_KEY="FunctionArn"
